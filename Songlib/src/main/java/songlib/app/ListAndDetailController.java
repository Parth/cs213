package songlib.app;

import java.util.List;

public class ListAndDetailController {

	// Song current song
	// Collection of songs

	public void setup() {
		// Read from file and populate global variables
		//render(result);
	}

	private void render() {

	}

	/*
		public void update(Action)
			what changed?
				if (list)
					update current song
				if field
					use equals method from song to see if song changed
					enable edit button
				if edit button && song changed
					update the collection - so the list changes the title
					call edit
				if deleteButton
					deleteSong
				if newSong
					newSong
	 */

	private void deleteSong(Song song) {

	}

	private void write(Song newSong) {

	}

	private void edit(Song oldSong, Song newSong) {

	}
}
